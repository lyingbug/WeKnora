# 长期记忆设计

> 对应 Roadmap 条目：`知识库形态 → 探索知识库与 Memory 结合的应用场景`（见 [`docs/ROADMAP.md`](./ROADMAP.md)）
>
> 本文描述已实现的第一期。写下它不是为了完整，而是为了让后面的人知道**每条约束为什么在这里**——这个功能在 WeKnora 里已经死过两次，两次的死因都记录在下面。

## 1. 它解决什么

在此之前，WeKnora 有三个容易被当成记忆、但都不是记忆的能力：

| 能力 | 位置 | 为什么不是长期记忆 |
|---|---|---|
| 多轮历史 | `internal/application/service/chat_pipeline/load_history.go` | 只在单个 Session 内有效，换个会话就没了 |
| Agent 上下文巩固 | `internal/agent/memory/consolidator.go` | 纯进程内，一次请求结束即消失 |
| 会话历史知识库 | `internal/application/service/message.go` 的 `IndexMessageToKB` | 租户级原始消息向量库，不属于某个人，无结构、无去重、无冲突处理 |

长期记忆补的是这一条：**跨会话地、属于某一个人地、可累积可纠正地**记住他说过的关于自己的事。

## 2. 前两次为什么失败

**第一次**（v0.3.2 引入，v0.7.1 由 PR #2130 整体删除）：基于 Neo4j 存 `:Episode` / `:Entity`，读路径先用 LLM 抽关键词再查图，写路径再调一次 LLM 抽实体关系。`MemoryService` 的每个方法都以 `repo.IsAvailable(ctx)` 开头，而 Neo4j 默认不开——对绝大多数用户，这个功能等价于不存在。除了一个设置开关外没有任何界面。

**第二次**（分支 `cursor/plan-long-term-memory-wiki-e57e`，未合并）：20082 行、111 个文件，一次性做了记忆页面化并复刻整套 Wiki 语义、记忆与知识库的锚点「点亮」层、桥接图谱、k-匿名洞察、分类型指数半衰期、44 个键的分层设置系统。它自己的文档列出了三十多条缺陷，其中最关键的一条是：聊天侧的可见性完全没有实现——记忆在对话里生效，但用户看不见。

**所以本设计有三条不可协商的约束：**

1. **不引入任何新依赖。** 只用主库（PostgreSQL / SQLite）和既有的任务队列抽象。没有 Neo4j，没有新的向量库，没有新的缓存。
2. **可见性是第一期交付物，不是后续增强。** 用户看不见、改不了、删不掉的记忆不能上线。
3. **范围优先于完备。** 第一期只做记忆本身；与知识库 / Wiki 的关联留到底座稳定之后。

## 3. 数据模型

两张表。

`memory_subjects` 是记忆空间，`(tenant_id, subject_id)` 唯一。`subject_id` 取 `Principal.StorageID()`，因此 Web 用户、IM 用户、API 外部用户、Embed 访客都能各自拥有空间而不需要 WeKnora 账号。它还缓存渲染好的常驻块和防抖时间戳。

`memory_items` 每行是一句话，字段中值得说明的是：

- `topic` 是抽取模型给出的可读主题（「在用的数据库」），**原样保存**。它是最好的检索线索：提问往往点名主题，而陈述句只带取值（「已经迁到 PostgreSQL」）。
- `normalized_key` 是 topic 归一化后的碰撞键，用于发现矛盾。它是排序去重后的字符串，**不能拿来做检索**（词序已被破坏）。
- `valid_from` / `invalid_at` / `superseded_by` 是双时间。矛盾信息不物理删除：旧条目写 `invalid_at`，新条目回填 `superseded_by`，于是「记忆为什么变了」在界面上可解释。这是从 Zep / Graphiti 借来的唯一一件事，图部分没有借。

作用域**永远从请求上下文推导**，没有任何接口接受 subject 参数。这不是为了少写校验，而是为了让「能不能改个 id 读到别人的记忆」这个问题根本不存在。

## 4. 读路径：零模型调用

每轮注入两段：

**常驻块**是渲染好的一段文本，直接从 `memory_subjects` 主键读出，只在写入时重算。内容是稳定特征（`profile` / `preference`），**加上所有用户显式要求记住的条目**——不论它是什么类型。后半条是手动验证逼出来的：用户说了「记住我的编辑器是 Neovim」，接着问「我平时用什么工具」，两句话没有一个词重叠，纯靠词法匹配就召不回来。用户唯一亲口要求记住的那条，恰恰最不该丢。

**情境召回**对 `fact` / `task` 做词法打分，取最相关的若干条。分词对 CJK 逐字切分并计算二元组（单个汉字「数」出现在数据、参数、数量里，噪声太大），相关度低于阈值的一律不注入——一条只是碰巧共享一个词的旧记忆，比没有记忆更糟。

第一期**不用向量**。单个空间的量级是几百条，词法足够，且不引入依赖。这一点写死在设计里，而不是设计写向量、实现改词法然后打补丁。等真实数据证明词法不够，再复用现有 `RetrieveEngineRegistry`，那时它是一个独立可验证的增量。

两条链路各一个接入点：RAG 走 `MEMORY_RECALL` 插件（`chat_pipeline/memory_recall.go`），Agent 走 `AgentEngine.SetMemoryPrompt`。Agent 侧必须走系统提示，因为 `buildMessagesWithLLMContext` 会丢弃来自历史的 system 消息——单独发一条记忆消息从第二轮起就静默消失了。

记忆内容包在 `<user_memory>` 标记里，并明确声明是背景数据而非指令。一旦用户写的句子进了系统提示，这是唯一能做的防线。

## 5. 写路径

**显式写入**是同步的、确定的：用户说「记住：……」或 `remember that ...`，前缀命中就原样落库，中间没有模型。这让默认档位 `explicit_only` 是有用的，而不只是安全的。

**隐式提炼**是异步的：会话结束后入队 `TypeMemoryExtract`（Lite 无 Redis 时走 `SyncTaskExecutor`），跑一次 LLM，输出 `ADD` / `UPDATE` / `DELETE` 决策（Mem0 的做法）。

抽取**只读 `role=user` 的消息**。助手自己的输出是模型在跟自己说话，把它喂回抽取，正是检索到的文档里的提示注入变成「关于用户的长期事实」的路径。

任务需要的一切都在 payload 里（tenant、subject、session、message、会话所用模型）。asynq 和 Lite 执行器都从一个空 context 起步，请求当时知道而 payload 没带上的东西，到 worker 手里就没了——上一版正是在这里静默失败。

### 5.1 覆盖性：开关开着时，没有一条消息会被丢掉

第一版的调度是拿当前时间和上次运行时间比，落在最小间隔内就直接 return。后果是那个窗口里的每一轮对话都被**静默丢弃**：功能看着是开的，实际什么都没学到。一次运行也只读触发它的那个会话最近的若干条消息，于是并行的第二个会话、以及一次说得太多的用户，都读不到。

现在覆盖性是**数据的性质，而不是时序的性质**：

- `memory_subjects.extract_cursor` 是水位线，运行时从它往后走。
- `pending_sessions` 是有未处理轮次的会话队列。一轮对话到来时若已有任务在途，它加入队列而不是被丢弃。
- `extract_scheduled_at` 是在途标记，让一串连发的对话只产生一个任务而不是每轮一个；标记会过期，避免 worker 崩溃后把这个人永久卡死。
- 队列在运行**开始时**就被取走。运行期间到来的轮次落进新队列并触发后继运行，而不会被这次没看见它们的运行抹掉。
- 一次运行读到消息上限、或运行期间又来了新轮次，都会排下一次运行。
- 运行失败时水位线不动——它没读成功的消息必须被重读，而不是被跳过。

最小间隔现在只**推迟**运行，从不丢弃轮次。它和挖掘延迟一样是工作空间设置项，不再是常量。

`internal/application/service/memory/coverage_test.go` 就是为这条性质写的：在防抖窗口内连发多轮、跨两个会话、一次说的比单次运行上限还多、运行失败，都断言每一句最终都被模型读到。把旧的丢弃逻辑放回去，这些测试会失败。

### 5.2 抽取质量：模型看到什么，以及它的回答怎么被解读

这一节的每一条都来自对照 mem0、Graphiti、LightMem 的实现，以及对运行中的系统写探针实证。

**分段。** 一次运行曾经把最多 40 条、可能跨两个会话、中间隔了一小时的消息拍平成一坨交给模型。现在按会话边界和长静默切成段，每段一次调用。超出单次运行上限的段留在水位线之前，由后继运行处理——所以分段不影响覆盖保证。LightMem 和 Nemori 都在抽取前先分段，这是抽取质量最容易垮掉的地方。

**前文上下文。** 一次运行只看得到新消息，于是「就用前面那个吧」到达时没有任何可解析的对象（探针实证过：提示词里只有这一句，已有笔记是 `(none)`）。现在把紧邻的几条用户消息作为只读上下文展示，明确标注不从中抽取。mem0 的抽取提示词里固定带 `last_k_messages`，同一个道理。

**用索引而不是主题字符串定位。** update/delete 现在给出已有笔记的**序号**，主题只作为兜底。mem0 把 uuid 映射成小整数传给模型，代码里直接注明是 anti-hallucination：模型少写一个字，靠字符串匹配就会静默变成新增。

**逐条溯源。** 每条决策带来源行号，落库时映射回具体消息。此前所有记忆都被记成"触发这次运行的那一轮"，在跨会话运行下会指向一个毫无关系的对话——这是引入多会话排空时改出来的回归。Graphiti 用 `episode_indices` 做同一件事。

**结构化输出。** 响应 schema 通过 `ChatOptions.Format` 下发，而不再只在提示词里描述形状。

**action 严格化。** 空 action 不再当作 add——一个被截断的响应曾经会变成静默写入。新增 `none`。

**运行内去重。** 同一次响应里两条同主题的决策不再互相 supersede。

**时间。** 每行带参考时间，提示词要求把「下周五」写成绝对日期；`task` 可以带 `expires_at`，过期后自动归档并停止召回。Graphiti 有一个专门的 `extract_timestamps` 提示词做相对时间解析，系统提示第一句就是 `NEVER hallucinate dates`。

**敏感信息。** 写路径在做任何事之前先脱敏：凭据、令牌、身份证、卡号、手机号。整条几乎只剩占位符的直接丢弃。这条不是可选项——记忆会被注入到之后每一轮的系统提示里，落进来的凭据不只是被留存，而是被反复发给模型。上一版的教训是过度脱敏同样是失败（长订单号被误伤、身份证却留了末三位），所以正例也写进了测试。

**工作空间自定义规则。** 追加到提示词里，用于产品猜不到的策略。mem0 有 `custom_fact_extraction_prompt`。

### 5.3 删除必须是真的删除

删除曾经不生效。显式的「记住 X」在这一轮结束时立刻落库，而防抖后的挖掘几分钟后读到同一条消息，于是把它又抽了一遍——探针实证：删除后 0 条，挖掘后 1 条。

现在删除会留下墓碑，**只存主题、语句指纹和来源消息，不存语句本身**：用户要求忘掉的东西，不该换张表继续留着。

只有指纹不够——挖掘复述时措辞会略有不同，哈希就不一样了（实测过：「我们的生产数据库是 X」被复述成「生产数据库是 X」）。所以还要按**来源消息**抑制，窗口一小时。窗口正是关键：它覆盖防抖后的那次重读，又不会把一条消息永久封禁，用户之后再说一遍来自新的消息，照常写入。

显式写入现在归因到**用户自己的消息**而不是回答，否则两条写路径对"这条记忆来自哪条消息"的认知不一致，抑制永远匹配不上。

去重有两层：同一 topic 键走 supersede；内容互相包含的走合并（「我们的生产库是 X」和「生产库是 X」是同一件事，但 topic 键不同，只靠键匹配会让用户看到重复的记忆）。

## 6. 遗忘：三条规则

不做衰减曲线（上一版把分类型指数半衰期算成了复利，profile 第 45 天就被归档）。只有：容量上限后按重要度与使用时间归档、用户手动删除、用户一键清空。每一条都能用一个断言测出来。

## 7. 可见性

- 设置 → 账户 → **我的记忆**：看、加、改、删、导出、一键清空，每条显示类型、来源与时间。
- 聊天回答上方：「本次使用了 N 条记忆」，作为**时间轴里的一个步骤**渲染，展开可见具体内容，悬停某行即可删除该条。

记忆节点复用 `RagPipelineProgress` 的树状节点结构，而不是自带一套卡片样式：召回记忆是这一轮回答前做的又一件事，给它单独的视觉语言只会让它读起来像与流程无关的东西，而且两套样式必然随时间漂移。纯聊天没有自己的流水线，但用到了记忆的那一轮仍有话要说，因此它渲染同一个时间轴组件，只是里面只有这一个步骤。

第二条不是加分项。用户注意到一条错误记忆的时刻，就是它影响了某个回答的时刻；让他在那一刻就能删掉，是这个功能能否被信任的分界线。

## 8. 配置：4 个键

工作空间开关（默认关）、写入方式（`explicit_only` / `auto`）、提炼模型（留空则用会话模型）、挖掘延迟、两次挖掘的最小间隔、每人容量上限；外加用户级开关和 Agent 级 `memory_enabled`。后两者**只能收紧**，不能放开工作空间的决定。

两个时间参数都有下界，且下界是成本考虑而不是安全考虑：延迟接近零会把一串连发的消息变成一次调用一条。

默认关是因为这个功能会留存用户说过的话。代价是它容易被忽略，所以空间设置页顶部用一段说明讲清楚开启后会发生什么——上一版「默认关 + 没有引导」等于功能不存在。

## 9. 验证方式

上一版复盘出的根因是「声明了但没接上，而测试进入的位置在接缝之下」。因此：

- 写路径的测试从门控进入，断言数据库里最终有什么，而不是直接调内部服务。
- 后台任务的测试用**空 context** 调用 handler，断言作用域是从 payload 重建的。
- 召回的测试断言最终 messages 里那段文本存不存在，而不只是 recall 返回了什么。
- `internal/application/service/memory/regression_test.go` 是中文跨会话回归集：记住画像、偏好常驻、按相关性召回、修正矛盾、续接在办事项、完成后不再召回、一次性提问不入库。
- `internal/application/service/memory/coverage_test.go` 断言开关开着时没有一条消息被丢掉，见 [5.1](#51-覆盖性开关开着时没有一条消息会被丢掉)。
- `internal/application/service/memory/quality_test.go` 覆盖 [5.2](#52-抽取质量模型看到什么以及它的回答怎么被解读) 和 [5.3](#53-删除必须是真的删除) 的每一条行为。

提示词好不好是个经验问题，读代码判断不了。`evalset.json` 是一份中文黄金集，`eval_test.go` 用它给提示词打分：默认离线运行，只校验集合本身没腐烂、每个用例的文本确实进了提示词；给一个真实模型就逐例输出通过与否。

```
WEKNORA_MEMORY_EVAL_MODEL=<model> WEKNORA_MEMORY_EVAL_BASE_URL=<url> \
WEKNORA_MEMORY_EVAL_API_KEY=<key> \
go test ./internal/application/service/memory/ -run TestPromptEval -v
```

刻意不设通过线：这个分数只有和同一份集合的上一次运行比才有意义，定一个门槛只会让人把门槛调低。

不使用 LoCoMo / LongMemEval 打分：其公开数字基本由厂商自跑，同一基准上不同来源相差数十个点，且没有与实际用法匹配的中文切分。

## 10. 第二期：让记忆改变检索

一期把记忆做成了「回答提示里多一段话」。这在通用助手里够用，在知识库产品里是把一半价值留在桌上：同一个问题，对做医学影像的人和对做自动驾驶的人，应该检索到不同的段落——而这个差别只能在检索**之前**施加，不能靠回答时补救。

二期围绕这一条展开，分成五块。

### 10.1 A 检索闭环：谁在问，会进入查询改写

`PluginQueryUnderstand` 在拼用户提示时追加一段 `<asker_background>`：身份、长期关注、常查资料标题。改写模型据此消解指代、补全检索词。

这里刻意**不做**知识库路由（按记忆裁掉某些知识库）。裁剪的收益是少检索几个库，代价是一条过期的记忆可能让这个季度的文档永远搜不到。对一个知识库产品来说，这个代价换不回来。所以记忆在这里只作为建议，不作为过滤器；真正的排序偏好交给 C 的重排加权，因为那一步是可加可减的连续量，最坏情况只是顺序略差，而不是内容不可达。

参与检索由空间级 `retrieval_conditioning` 控制，默认开。关掉它不会关掉记忆本身——回答提示里的记忆是另一个开关。

### 10.2 B 关注面：一次提问是噪声，反复提问是信号

一期的抽取规则是「一次性提问不入库」。这条规则挡掉了噪声，也挡掉了知识库产品里最主要的用户信号：人们大部分时间都在提问，而问什么本身说明了他是谁。

二期的做法不是放宽这条规则，而是把它拆成两层：

- **主题只计数，不入库。** 抽取模型除了 `memories` 还返回 `topics`，写进 `memory_topic_stats`。同一主题在不同会话里被问到 N 次（默认 3，可配）后，才提升为一条 `interest` 记忆。这样「上海有什么好吃的」问一次不留痕，问三次才成为「关注上海餐饮」。
- **推断进收件箱，不直接生效。** 模型可以给一条记忆打 `inferred`，表示这是对用户的推断而非复述。这类记忆以 `pending` 状态落库：在「我的记忆」里可见、可确认、可否决，但**永远不会进入任何提示词**，也不参与检索调节。

#### 主题识别：不要把模型吐出的字符串当主键

这里有个一开始做错了的地方，值得单独写下来。

第一版直接拿模型返回的主题字符串算键，而那个键函数是「把字符去重后排序」。实测的结果：

| 主题 | 算出的键 |
|---|---|
| `儿童游泳赛事组织` | `事-儿-泳-游-童-组-织-赛` |
| `儿童游泳赛事的组织` | `事-儿-泳-游-的-童-组-织-赛` |
| `少儿游泳比赛筹办` | `儿-办-少-比-泳-游-筹-赛` |

多一个「的」字就是另一个主题。也就是说阈值只有在模型**逐字重复**时才可能达到——功能开着，但永远学不到东西。而模型给同一件事命名，本来就不会两次都一样。

mem0 和 Graphiti 在这个问题上给的是同一个答案：**永远不要信任表层字符串，要把它解析到已有实体上**。mem0 的实体入库是「精确匹配 or 向量相似度 ≥ 0.95」，并把已有记忆塞进抽取提示让模型复用；Graphiti 是三层——归一化精确匹配 → MinHash/LSH 的 Jaccard 模糊匹配（低信息量的名字直接跳过这层，避免误合并）→ LLM 判定。

我们按同样的分层做，从便宜到贵：

| 层 | 手段 | 能解决 | 成本 |
|---|---|---|---|
| 0 | 抽取提示里列出该用户已有主题，要求原样复用 | 大部分漂移 | 0 |
| 1 | 归一化精确匹配 + 别名表 | `儿童游泳赛事的组织` | 0 |
| 2 | 字符 2-gram Dice ≥ 0.80，短标签跳过 | `游泳赛事组织` | 0 |
| 3 | 一次批量模型判定 | `少儿游泳比赛筹办` | 1 次小调用 |

几个具体判断：

- **归一化改成保序**。老的字符袋键在两个方向上都错：一个字之差算不同主题，而两个词的异位构词算同一个主题。新的 `NormalizeTopicKey` 保留字序，只去掉真正没有信息量的字（的/了/在/是……）和结尾的限定词（问题/方面/情况……）。`NormalizeMemoryKey` 没动——记忆条目那边有包含关系兜底，而主题这边原本什么兜底都没有。
- **用 Dice 而不是 Jaccard**，因为常见的漂移是一方比另一方长（`游泳赛事组织` vs `儿童游泳赛事组织`），Dice 对这种情况更宽容。
- **阈值定在 0.80，偏保守**。合并错了会把两件事的计数混在一起，而且事后完全看不出来；没合并只是晚一点提升，第三层还会兜住。这和 Graphiti 把确定性层卡在 0.9 Jaccard 是同一个考虑。
- **短标签跳过第 2 层**。两个字的标签上，一个共享 bigram 就是大半个分数，模糊匹配基本只会制造误合并。Graphiti 用香农熵做同样的事。
- **模型只有否决权，没有自由裁量权**。第 3 层只被问到前两层没解决的标签，它对没被问到的标签的回答一律忽略——否则一个乱答的模型可以推翻更可靠的层已经做出的判断。
- **判过一次就不再判**。模型认可的说法作为别名存下来，下次直接命中第 1 层。

没有用向量相似度，虽然 mem0 用的是它。原因是记忆服务里没有接嵌入模型，自动从模型列表里挑一个不可靠，而且相似度阈值随嵌入模型变化；抽取器手上本来就有一个 chat model，一个人的主题表又只有几十条，批量问一次比引入一条新依赖更简单也更准。如果第 3 层将来被调用得很频繁，换成嵌入是自然的升级方向。

第二点是这一期最重要的设计判断。推断出「你可能在组织儿童游泳赛事」这种信息，价值远高于复述用户说过的话，风险也同样高。ChatGPT 的做法是把这类推断放进一个用户看不见的背景层；我们的判断是，在一个企业知识库产品里，一条错误的、被默默当成事实的推断，足以让整个记忆功能失去信任。所以推断照做，但要当着用户的面做。

否决会留下墓碑，所以同一个推断不会下周再来一次。

### 10.3 C 文档亲和：这个人常从哪些材料里得到答案

`memory_doc_affinity` 记录每个人的回答引用过哪些文档，`PluginMemoryAffinity` 在重排后据此加权。

这个信号是弱的——引用只说明检索器一直挑中它，不说明用户觉得它有用——所以加权上限只有 1.15，且随次数对数饱和：第十次复用的权重远小于第二次。理由是，如果熟悉度可以无限放大，一个人最早打开的那份文档会赢下他此后的所有提问，那比不做个性化更糟。

上一版留下的教训是「建了锚点表，四个消费方全都把它过滤掉了」。所以这一期的规矩是：**每人检索信号必须和读它的代码同期上线**，表和消费方在同一个提交里。

### 10.4 D 巩固与时间重写

抽取每次只看最新一段对话，这对单轮是对的，但没有任何一次调用能发现「三周里五轮对话把同一条偏好记了五个说法」，或者「上季度那个在办事项现在只是噪声」。

因此加一个整库回顾，每人每天至多一次，搭在后台抽取任务的尾巴上运行（Lite 模式没有定时器，这样两种部署行为一致）：

- **合并**：同类且词面重叠超过 0.55 的记忆交给模型合成一条，原记录以 `superseded` 保留而非删除，用户仍能看到它曾经是什么、什么时候变的。阈值定得偏高是有意的——错误合并会毁掉用户真的说过的话，漏合并只是留点冗余。
- **时间重写**：过期的直接归档；45 天无人提起的在办事项**降权而不删除**。用户从没说过他做完了，所以我们没有资格替他删掉；降到最低重要度即可让它退出常驻块、并在容量满时最先被淘汰。

### 10.5 E 会话历史检索

把对话蒸馏成几十句话必然丢细节——「三周前你给我的那个配置」不是关于这个人的持久事实，存成记忆是错的形状。正确的分工和 MemGPT 一样：小而常驻的摘要 + 按需回到全文检索。

因此给 Agent 加了 `search_conversations` 工具，检索**调用者自己的**历史会话。

做这件事时发现了一个既有缺陷：会话列表一直是按用户隔离的（`GetByTenantID(ctx, tenantID, userID)`），但会话搜索只按租户过滤。也就是说，工作空间里任何一个 viewer 在搜索框里输入同事的项目名，就能读到同事的私人对话。这一期把搜索的作用域改成和列表一致，包括历史遗留的 tenant 级会话的兼容规则。工具的归属 ID 取自调用者身份而非模型参数，因此没有任何提示词能把它指向别人的对话。

### 10.6 模型调用：关掉思考，并且不许把截断当成「没什么可记的」

线上 trace 里出现过这样一条：输入很短，`max_completion_tokens=1200`，返回 `content=""`、`finish_reason=length`、completion 用掉 1201。

原因不是预算不够，是抽取调用没关思考。思考型模型的 reasoning 计入 completion，于是预算全花在推理上，一个字都没轮到输出。这个仓库里其他所有结构化调用（`query_understand`、`extract_entity`、`graph`、`knowledge_process`、`message_suggestion`……）都显式关了思考，只有记忆的三个调用漏了——这是照着惯例做就不会有的问题。

但更严重的是它后面那一段。空响应走的是 `parseExtractionResponse("")`，返回空结果且**不报错**，于是：

1. `applyDecisions` 什么都不做；
2. 水位线照常前进；
3. 这批消息被标记为已读，永远不会被重读。

也就是说，功能开着、任务成功、日志干净，而「开关开着就没有一条消息会被丢掉」这个保证在模型截断时是失效的——而且是静默失效。这比 token 用超本身糟得多。

现在的处理分三种情况：

- **截断**（`finish_reason=length`，或内容为空）：先用更大的预算重试一次。有些模型无视关闭思考的参数，给够地方放推理它才答得出来。
- **重试后仍然截断**：返回错误。错误会让水位线停在原地，这批消息下次重读，而不是被一次什么都没读到的运行消费掉。
- **完整但格式错**：仍然丢弃并前进。同一个提示在 temperature=0 下重试只会得到同样的垃圾，卡住这个人的水位线换不来任何东西。

区分第二和第三种是这里唯一需要判断的地方：截断是预算/配置问题，可恢复；格式错是模型能力问题，不可恢复。把两者都当成可重试会让一个坏模型永久卡住一个用户，都当成不可重试就会丢消息。

### 10.7 二期新增的验证

- `chat_pipeline/memory_retrieval_test.go`：改写提示里确实带上了背景；没有记忆时提示逐字不变；熟悉文档在分数接近时排到前面，在分数明显落后时**不会**被拖上来。
- `memory/topic_resolve_test.go`：三层解析各自的边界——一字之差走精确层、长短之差走模糊层、同义词走模型层且判过就记别名；同产品名的不同话题不合并；短标签不做模糊匹配；模型不能推翻更便宜的层；一次运行里的两个新说法不会建两行。
- `memory/retrieval_test.go`：pending 记忆永不进提示也不参与检索；否决后不再复现；主题计数到阈值才提升且只提升一次；关注与亲和不跨人、不跨空间；过期任务降权而非删除；只有同类且高度重叠的记忆才会被合并。
- `memory/coverage_test.go` 新增：后台 worker 在**没有请求上下文**的情况下必须能完成主题计数。这条是端到端跑出来的真实缺陷——计数从 context 重新解析作用域，而 worker 的 context 里没有身份，于是抽取一切正常、记忆照常写入、关注面永远是空的。
- `repository/message_owner_scope_test.go`：搜索的归属过滤，含跨人、跨空间和遗留会话三种情况。
- `memory/coverage_test.go` 新增：模型返回空内容 + `finish_reason=length` 时，水位线不得前进，且模型恢复后同一条消息仍会被蒸馏；截断会用更大预算重试；抽取调用必须显式关闭思考（留空等于交给模型决定，这正是它出问题的原因）。

## 11. 后续

- 跨渠道身份绑定：同一个人在 IM 与 Web 上是两个 `StorageID`。
- 记忆的向量召回：当前是词面匹配，够快够可解释，但换个说法就召不回。
- 与 Wiki 的关联。
